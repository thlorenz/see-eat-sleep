<!DOCTYPE html>
<html>
<head>
  {{> ses-core-head }}
  {{> ses-see-head }}
  {{> ses-eat-head }}
  {{> ses-sleep-head }}
  {{> ses-aside-head }}
  {{> ses-app-head }}
</head>
<body>
  <div class="aside">
    {{> ses-aside-main aside }}
  </div>

  <div class="wrapper">
    {{> ses-core-header }}
    {{> ses-see-main see }}
    {{> ses-eat-main eat }}
    {{> ses-sleep-main sleep }}
  </div>

  {{> ses-core-scripts-body-js }}
</body>
</html>
