<!DOCTYPE html>
<html>
<head>
  {{> ses-core-head }}
  {{> ses-see-head }}
  {{> ses-eat-head }}
  {{> ses-sleep-head }}
  {{> ses-aside-head }}
</head>
<body>
  {{> ses-core-header }}
  {{> ses-see-main see }}
  {{> ses-eat-main eat }}
  {{> ses-sleep-main sleep }}
  {{> ses-aside-main aside }}

  {{> ses-core-scripts-body-js }}
</body>
</html>
